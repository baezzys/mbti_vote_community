spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mbti?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: otp19407312@!
  jpa:
    database-platform: org.hibernate.dialect.MariaDB103Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Seoul

mbti:
  app:
    jwtSecret: ======================mbti=Spring===========================
    jwtExpirationMs: 86400000
